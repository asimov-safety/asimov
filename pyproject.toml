[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "asimov-conformance-prototype"
version = "0.2.0a2"
description = "Asimov draft and report aggregation prototype; not a certification service"
readme = "README.md"
requires-python = ">=3.10"
license = {text = "Apache-2.0"}
dependencies = []

[project.scripts]
asimov-draft = "asimov_conformance.__main__:main"

[tool.setuptools.packages.find]
include = ["asimov_conformance*"]

[tool.setuptools.package-data]
asimov_conformance = ["catalog.json"]
